"""AI governance extension (docs/ai_extension/DESIGN.md).

Jobs collect and reconcile; the app only reads derived tables and writes
steward decisions. Nothing in this package calls external APIs from the app.
"""
